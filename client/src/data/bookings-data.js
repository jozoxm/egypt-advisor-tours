// ============================================
// BOOKINGS DATA FILE
// ============================================
// Booking records are managed server-side only (server/data/bookings.json).
// This file is kept as an empty fallback so the client bundle does not
// contain customer PII.  The Admin Panel always loads bookings from the API.

export const bookings = [];
