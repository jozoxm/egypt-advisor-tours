# ⚠️ Wrong Directory!

You're in the **client/** directory. 

## To Run Commands:

**Go back to the repository root:**

### Windows:
```cmd
cd ..
```

### Mac/Linux:
```bash
cd ..
```

## Then Run These Commands:

```bash
# Install all dependencies
npm run install:all

# Or install just client dependencies
npm run install:client

# Start the client development server
npm run start:client

# Build the client for production
npm run build
```

## 📍 Current Location
You are here: `egypt-advisor-tours/client/`

## ✅ Correct Location
You need to be here: `egypt-advisor-tours/`

## Quick Navigation

```bash
# Go to root directory
cd ..

# Check you're in the right place (should show package.json)
ls package.json

# Now run your commands
npm run install:client
npm run start:client
```

---

**Note:** All npm scripts are defined in the **root** package.json, not in this client directory.
